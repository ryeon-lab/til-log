package com.tillog.til_log.repository;

import com.tillog.til_log.domain.DefTilConnect;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DefTilConnectRepository extends JpaRepository<DefTilConnect, Long> {
}
